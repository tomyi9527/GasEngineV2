(function ()
{
    let UITreeView = function(options) 
    {
        options = options ? options : {};
        mgs.UIBase.call(this, options);

        
    };
    mgs.classInherit(UITreeView, mgs.UIBase);
}());