(function ()
{
    let UISelectArea = function(options) 
    {
        options = options ? options : {};
        mgs.UIBase.call(this, options);

        
    };
    mgs.classInherit(UISelectArea, mgs.UIBase);
}());